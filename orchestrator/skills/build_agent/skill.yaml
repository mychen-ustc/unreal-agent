name: build_agent
description: "通过评审后打平台包触发 cook/run"
default_tier: default

tier: 2
distill_visibility: hidden

input_schema:
  type: object
  properties:
    task: { type: string, description: "任务" }
output_schema:
  type: object
  properties:
    result: { type: object }

tool_whitelist:
  - build_cook_run
  - rag_search
  - project_list_directory

risk: mutating
