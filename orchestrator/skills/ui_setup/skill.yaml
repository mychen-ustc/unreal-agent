name: ui_setup
description: "生成 UMG Widget(HUD/交互)绑定 DataTable"
default_tier: default

tier: 2
distill_visibility: lite

input_schema:
  type: object
  properties:
    task: { type: string, description: "任务" }
output_schema:
  type: object
  properties:
    result: { type: object }

tool_whitelist:
  - ui_create_umg_widget
  - data_validate_rows
  - rag_search
  - project_list_directory

risk: mutating
