# 由 gen 脚本生成（对齐 Common Spec）
- id: plan
  severity: read_only
  tier: fast
  tool: rag_search
- id: produce
  dependencies: [plan]
  severity: mutating
  tier: def
  tool: asset_generate_3d
  tool_args:
    skill: asset3d_generator
- id: verify
  dependencies: [produce]
  severity: read_only
  tier: fast
  tool: data_validate_rows
