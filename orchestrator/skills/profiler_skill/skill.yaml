name: profiler_skill
description: "跑 GPU/CPU Profiler 生成性能超标报告"
default_tier: default

tier: 2
distill_visibility: full

input_schema:
  type: object
  properties:
    task: { type: string, description: "任务" }
output_schema:
  type: object
  properties:
    result: { type: object }

tool_whitelist:
  - profiler_report
  - rag_search
  - project_list_directory

risk: mutating
