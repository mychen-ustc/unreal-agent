# 由 gen 脚本生成（对齐 Common Spec）
- id: plan
  severity: read_only
  tier: fast
  tool: rag_search
- id: produce
  dependencies: [plan]
  severity: mutating
  tier: def
  tool: profiler_report
  tool_args:
    skill: profiler_skill
- id: verify
  dependencies: [produce]
  severity: read_only
  tier: fast
  tool: project_list_directory
