name: s1_market_research
description: "拉取市场数据/趋势/画像，产出市场报告到 /strategy/market_report"
default_tier: default

tier: 2
distill_visibility: full

input_schema:
  type: object
  properties:
    task: { type: string, description: "任务" }
output_schema:
  type: object
  properties:
    result: { type: object }

tool_whitelist:
  - rag_search
  - report_write
  - project_list_directory

risk: mutating
